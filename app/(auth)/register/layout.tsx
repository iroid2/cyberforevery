import type React from "react";

export const metadata = {
  title: "Cyber4Every1 — Create Account",
};

export default function RegisterLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
